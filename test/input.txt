String hello = "Hello world";

double getSum(int a, int b, double c) {
  return (a + b) * c;
}

void init() {
  double sum = getSum(2, 1, 2.0);
  writeLine(sum);
  writeLine(hello);
}

start with init();