// Comentario
/* Comentario
multi linea
*/

int vector[] = new int[3];
double vector2[] = { 2.2, 5.3, 6.8, vector[1] };
String alex = (String) !((("texto" * 2 + vector2[1]) + -(vector[0] + 2)/3) + "hola");
double dbl = 2.3;
vector[2] = 5;
dbl++;
dbl--;

DynamicList<int> myList = new DynamicList<int>;
String myStr = new String();
append(myList, 3);
getValue(myList, 0);
setValue(myList, 0, 34);

if((1 + 2)*2 == 6) {
    append(myList, 5);
}

int edad = 18;


/* Salida esperada
Tengo 18 anios.
No se que edad tengo. :(
*/

int edad = 18;
switch( edad ) {
    case 10:
        append(myList,"Tengo 10 anios.");
        break;
    case 18:
        append(myList, "Tengo 18 anios");
    case 25:
        append(myList, "Tengo 25 anios");
        break;
    default:
        append(myList, "No se que edad tengo");
        break;
}


for (int i=0; i<3; i++){
    append(myList, "No se que edad tengo");

    for (int i=0; i<3; i++){
        if((1 + 2)*2 == 6) {
            append(myList, 5);
            break;
        }
    }
}

Int a=5;
do{
 If (a>=1 && a <3){
     append(myList, 5);
 }
 Else{
      append(myList, 5);
      break;
 }
 a--;
} while (a>0);


//Ejemplos
 //--> Dentro de un metodo

 int i;
 for(i = 0; i < 9; i++){
 if(i==5){
 return; //se detiene
 }
   append(myList, 5);
 }
 
