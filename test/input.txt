String hello = "Hello world";

int getSum(int a, int b) {
  return a + b;
}

void init() {
  int num[] = { 1, getSum(1, 1), 3, 4 };
  num[0] = 7;
  int newValue = getSum(num[0], 1) == 3 ? 90 : 7;
  num[getSum(1, 1)] = newValue;
  writeLine(num);
}

start with init();